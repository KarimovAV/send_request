BASE_URL = 'https://send-request.me'

# USERS for GET, POST, additionally to add {user_id} for  PUT, DELETE, GET
users_end_point = '/api/users/'

# COMPANIES for GET, additionally to add {user_id} for GET
companies_get_endpoint = '/api/companies/'

# ISSUES for GET, additionally to add id
issues_companies_end_point = '/api/issues/companies/'
issues_users_end_point = '/api/issues/users/'

# ISSUES for POST
issues_post_users = '/api/issues/users/'

# AUTH for GET, POST
auth_post_end_point = '/api/auth/authorization/'
auth_get_end_point = '/api/auth/me/'

